# handoff — agent instructions

Read `README.md` for intent, then `CLAUDE.md` — it is the single source of
working notes for any coding agent in this repo (architecture, invariants,
test/bench commands, command contracts). Nothing here overrides it.
